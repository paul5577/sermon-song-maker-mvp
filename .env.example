# Worker polling interval (milliseconds)
WORKER_POLL_MS=1800

# Optional: absolute path to a Korean-capable font on the render worker.
# Linux example: /usr/share/fonts/truetype/nanum/NanumGothic.ttf
RENDER_FONT=
